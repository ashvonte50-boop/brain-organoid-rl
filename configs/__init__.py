"""
configs — YAML configuration files and schema validation utilities.

Planned modules:
    schema    — Pydantic / OmegaConf schema for type-safe config validation
    sweeps    — WandB sweep YAML templates for hyperparameter search
"""
